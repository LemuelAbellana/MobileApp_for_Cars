# HTTP is for explicitly acknowledged local testing only; HTTPS was unreachable.
EXPO_PUBLIC_CARS_API_URL=http://lemzy.duckdns.org/cars.php

# LOCAL DEMO ONLY: client-bundled values are extractable, not production secrets.
EXPO_PUBLIC_DEV_CARS_BEARER_TOKEN=replace_me_locally
